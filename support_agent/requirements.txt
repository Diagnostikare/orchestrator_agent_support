# Dependencias de RUNTIME del agente desplegado.
# Vive en el dir del agente porque `adk deploy` lo busca aca
# (adk/cli/cli_deploy.py:728); en la raiz se ignora y genera uno propio
# con solo google-adk, que fue lo que rompio el primer deploy.
google-adk==2.7.1
# `buscar_documentacion` ejecuta la busqueda client-side contra Discovery
# Engine (ver tools/docs_search.py) — sin esto el import del modulo falla.
google-api-core==2.34.0
google-cloud-discoveryengine==0.20.2
